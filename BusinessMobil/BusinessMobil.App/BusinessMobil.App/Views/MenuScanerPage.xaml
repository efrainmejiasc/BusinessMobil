<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewModel="clr-namespace:BusinessMobil.App.ViewModel"
             x:Class="BusinessMobil.App.Views.MenuScanerPage">
    <ContentPage.BindingContext>
        <viewModel:MenuScanerViewModel/>
    </ContentPage.BindingContext>
    <ContentPage.Content>
        <Grid Style="{StaticResource MargenContentPage}" VerticalOptions="CenterAndExpand">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackLayout Grid.Row="0" Style="{StaticResource MargenButton}">
                <Button Text="Agregar Asistencia" Command="{Binding AgregarAsistenciaCommand}"/>
            </StackLayout>

            <!--<StackLayout Grid.Row="1" Style="{StaticResource MargenButton}">
                <Button Text="Agregar Asistencia"/>
            </StackLayout>-->

            <!--<StackLayout Grid.Row="3" Style="{StaticResource MargenButton}">
                <Button Text="Agregar Asistencia"/>
            </StackLayout>-->
            
        </Grid>
    </ContentPage.Content>
</ContentPage>
